{{define "error" -}}

    {{template "head" . -}}

    <p id="err-msg">{{.Data.ErrText -}}</p>

    <p>
	{{if .Data.PrevUrl -}}
	    <a href="{{.Data.PrevUrl}}">Go Back</a>&nbsp;&nbsp;
	{{- end -}}
	<a href="/">Go Home</a>
    </p>

    {{template "foot" . -}}

{{end -}}
